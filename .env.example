# Copy this file to .env (or use these keys when configuring Render / your host)
# Replace placeholder values with your real credentials before use.

MEILI_HOST=http://localhost:7700
MEILI_API_KEY=masterKey

# nickname provider options: graph | cloudflare | hybrid
NICKNAME_PROVIDER=hybrid

# Cloudflare (only required if using cloudflare or hybrid)
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id
CLOUDFLARE_API_TOKEN=REPLACE_WITH_TOKEN
CLOUDFLARE_AI_MODEL=gpt-4o-mini

# Optional path to local nicknames file (inside container)
NICKNAMES_PATH=/app/nicknames/nicknames.json
